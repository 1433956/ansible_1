- name: print values using loop
  hosts: local
  connection: local
  tasks:
  - name: print values with loop
    ansible.builtin.debug:
      msg: " hi!, hello :: {{ item }}"
    loop:
    - ramesh
    - suresh
    - ramu

