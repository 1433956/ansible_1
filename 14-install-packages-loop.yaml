- name: install package using loop
  hosts: local
  connection: local
  tasks:
  - name: looping the packages
    ansible.builtin.package:
      name: "{{ item }}"
      state: present
    loop:
    - mysql
    - zip
    - python3



